/* animated background */
var animation = lottie.loadAnimation({
    container: document.find('#animBack','#health-container'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'data.json'
});
