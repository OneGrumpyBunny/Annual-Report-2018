/* animated background */
var animation = lottie.loadAnimation({
    container: document.getElementById('animBack', 'health-container'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'data.json'
});
