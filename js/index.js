/* animated background */
var animation = lottie.loadAnimation({
    container: document.getElementById('mainBack'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'data.json'
});
